service cloud.firestore {
  match /databases/{database}/documents {
    match /posts/{post} {
      allow read, write: if request.auth != null; // Allow read/write if user is authenticated
    }
  }
}